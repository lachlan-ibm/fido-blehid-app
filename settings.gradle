/*
 * Copyright IBM 2025
 */

rootProject.name = 'fido_blehid_app'

// Include subprojects
include ':app'
include ':lib'

// Optional: Define project directories if they're not in default locations
// project(':app').projectDir = file('app')
// project(':lib').projectDir = file('lib')

// Made with Bob
