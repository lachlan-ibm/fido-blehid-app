<?xml version="1.0" encoding="utf-8"?>
<!--
 Copyright IBM 2025
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
                     android:usesPermissionFlags="neverForLocation" />
    <!-- Classic Bluetooth permissions for Android 11 and below -->
    <uses-permission android:name="android.permission.BLUETOOTH"
                     android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
                     android:maxSdkVersion="30" />

    <!-- Declare Classic Bluetooth feature -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />
    <!-- Declare BLE feature for FIDO service -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Boot receiver permission -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Wake lock: keeps CPU alive while UP callback posts its notification -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Full-screen intent: required on Android 14+ to show UP prompt over lock screen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Vibration feedback for QR code scanning -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Camera permissions for QR code scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".AyeBleKeyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/test_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/test_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name="com.isfs.blekey.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Register ManageActivity -->
        <activity
            android:name="com.isfs.blekey.activity.ManageActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
            
        <!-- Register ResidentCredentialsActivity -->
        <activity
            android:name="com.isfs.blekey.activity.ResidentCredentialsActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
            
        <!-- Register CreatePasskeyActivity -->
        <activity
            android:name="com.isfs.blekey.activity.CreatePasskeyActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
            
        <!-- Register CredentialIssuanceActivity -->
        <activity
            android:name="com.isfs.blekey.activity.CredentialIssuanceActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
            
        <!-- Register CredentialPresentationActivity -->
        <activity
            android:name="com.isfs.blekey.activity.CredentialPresentationActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
            
        <!-- Register QRScannerActivity -->
        <activity
            android:name="com.isfs.blekey.activity.QRScannerActivity"
            android:exported="false"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.NoActionBar" />
            
        <!-- UV PIN Entry Activity (GET_TKN_UV / built-in UV) -->
        <activity
            android:name="com.isfs.blekey.activity.UvPinEntryActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Advanced Configuration Activity -->
        <activity
            android:name=".activity.AdvancedConfigActivity"
            android:label="@string/adv_config_title"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.MaterialNoActionBar" />

        <!-- Deep Link Handler for QR Code Scanning -->
        <activity
            android:name="com.isfs.blekey.activity.CredentialHandlerActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            
            <!-- Credential Offer Deep Link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="openid-credential-offer" />
            </intent-filter>
            
            <!-- Presentation Request Deep Link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="openid4vp" />
            </intent-filter>
            
            <!-- Generic OpenID Deep Link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="openid" />
            </intent-filter>
        </activity>
            
        <!-- Single CTAP foreground service — owns both HID and BLE GATT transports -->
        <service
            android:name=".service.BluetoothCtapService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Digital Credential Provider Service -->
        <!-- Note: Full Credential Manager integration requires Android 14+ (API 34+) -->
        <service
            android:name=".credentials.DigitalCredentialProviderService"
            android:enabled="true"
            android:exported="false">
            <meta-data
                android:name="android.credentials.provider"
                android:resource="@xml/digital_credential_provider" />
        </service>

        <!-- Boot receiver to auto-start service on device boot and Bluetooth state changes.
             android:exported="true" is required on Android 14+ (API 34+): the system delivers
             BOOT_COMPLETED from a separate process context and will not send it to a receiver
             with exported="false".  The declared intent-filter only lists system-protected
             broadcasts (BOOT_COMPLETED, MY_PACKAGE_REPLACED, STATE_CHANGED) that third-party
             apps cannot send, so no security risk is introduced. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>


</manifest>
